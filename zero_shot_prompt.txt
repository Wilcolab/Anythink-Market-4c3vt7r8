Zero-shot Prompt Template
-------------------------

Task: [Describe the task concisely]

Audience: [Optional — target user or tone]

Constraints: [e.g., length limits, format, style, or prohibited content]

Input: [Any input data the model should use]

Desired output: [What to return and exact format, e.g., plain text, JSON]
def text_to_snake_case(text):
    """Convert text to snake_case format."""
    import re
    # Replace spaces and hyphens with underscores
    text = re.sub(r'[\s\-]+', '_', text)
    # Insert underscore before uppercase letters and convert to lowercase
    text = re.sub(r'(?<!^)(?=[A-Z])', '_', text).lower()
    # Remove any non-alphanumeric characters except underscores
    text = re.sub(r'[^a-z0-9_]', '', text)
    # Remove consecutive underscores
    text = re.sub(r'_+', '_', text)
    return text.strip('_')
Examples (optional):
- Input: [example input]
  Output: [expected output]

Notes: Use precise, unambiguous instructions. Keep outputs concise and follow the requested format exactly.

Created: 2025-12-18